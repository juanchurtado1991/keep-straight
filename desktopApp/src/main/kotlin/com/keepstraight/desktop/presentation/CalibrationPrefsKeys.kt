package com.keepstraight.desktop.presentation

/** SharedPreferences keys for landmark calibration blobs (desktop). */
object CalibrationPrefsKeys {
    const val VALID = "cal_valid"
    const val AT_MS = "cal_at"
    const val PREFIX_ERECT = "erect"
    const val PREFIX_SLUMP = "slump"
}
